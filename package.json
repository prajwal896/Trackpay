{
  "name": "hourglass",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run build && npm start --prefix server",
    "dev": "npm run dev:hot --prefix server"
  }
  
}
